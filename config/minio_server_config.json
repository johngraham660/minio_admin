{
  "buckets": [
    "concourse-pipeline-logs",
    "jenkins-pipeline-logs",
    "concourse-pipeline-build-artifacts",
    "jenkins-pipeline-build-artifacts",
    "k8s-etcdbackup-production"
  ],
  "users": [
    {
      "username": "${MINIO_USER_CONCOURSE}",
      "vault_path": "secret/data/minio/users",
      "policy": "concourse-pipeline-artifacts-policy.json"
    },
    {
      "username": "${MINIO_USER_JENKINS}",
      "vault_path": "secret/data/minio/users",
      "policy": "jenkins-pipeline-artifacts-policy.json"
    },
    {
      "username": "${MINIO_USER_K8S}",
      "vault_path": "secret/data/minio/users",
      "policy": "k8s-etcdbackup-policy.json"
    }
  ]
}
